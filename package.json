{
  "name": "yurian-omnix",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm --filter @omnix/api build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "node --test",
    "typecheck": "tsc --noEmit",
    "start": "node apps/api/dist/main.js"
  }
}
