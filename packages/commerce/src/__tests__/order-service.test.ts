import { createOrderService } from "../order-service";
describe("order service",()=>{it("rejects unknown order",async()=>{const repo={get:async()=>null,create:async()=>{throw new Error("unexpected")},updateStatus:async()=>{throw new Error("unexpected")}};await expect(createOrderService(repo).update("t","missing",{status:"CONFIRMED"})).rejects.toThrow("Order not found");});it("rejects invalid transition before persistence",async()=>{let called=false;const repo={get:async()=>({id:"o",tenantId:"t",customerId:"c",status:"FULFILLED",items:[]} as any),create:async()=>({} as any),updateStatus:async()=>{called=true;return {} as any}};await expect(createOrderService(repo).update("t","o",{status:"PENDING"})).rejects.toThrow("Invalid order transition");expect(called).toBe(false);});});
