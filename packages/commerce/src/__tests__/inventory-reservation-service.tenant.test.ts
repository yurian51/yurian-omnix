import { createInventoryReservationService } from "../inventory-reservation-service";
describe("inventory reservation tenant boundary",()=>{it("forwards tenant id on release and consume",async()=>{const calls:string[]=[];const repo={get:async(tenantId:string)=>{calls.push(`get:${tenantId}`);return {id:"r",status:"RESERVED" as const}},reserve:async()=>({id:"r",status:"RESERVED" as const}),updateStatus:async(tenantId:string,id:string,status:string)=>{calls.push(`update:${tenantId}:${id}:${status}`);return {} as any}};const service=createInventoryReservationService(repo as any);await service.release("tenant-a","r");await service.consume("tenant-a","r");expect(calls).toEqual(["get:tenant-a","update:tenant-a:r:RELEASED","get:tenant-a","update:tenant-a:r:CONSUMED"]);});});
