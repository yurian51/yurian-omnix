import { withTransaction } from "../transaction";
describe("transaction boundary",()=>{it("commits successful work",async()=>{let committed=false;const runner={run:async(work:any)=>{const tx={commit:async()=>{committed=true},rollback:async()=>{}};const value=await work(tx);await tx.commit();return value;}};await expect(withTransaction(runner,async()=>"ok")).resolves.toBe("ok");expect(committed).toBe(true);});it("rolls back failed work",async()=>{let rolledBack=false;const runner={run:async(work:any)=>{const tx={commit:async()=>{},rollback:async()=>{rolledBack=true}};try{return await work(tx);}catch(e){await tx.rollback();throw e;}}};await expect(withTransaction(runner,async()=>{throw new Error("boom")})).rejects.toThrow("boom");expect(rolledBack).toBe(true);});});
