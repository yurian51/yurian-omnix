import { createInventoryReservationService } from "../inventory-reservation-service";
describe("inventory reservation service",()=>{it("releases only reserved inventory",async()=>{const repo={get:async()=>({id:"r1",tenantId:"t",orderId:"o",productId:"p",quantity:1,status:"RELEASED" as const}),reserve:async()=>({id:"r1",tenantId:"t",orderId:"o",productId:"p",quantity:1,status:"RESERVED" as const}),updateStatus:async()=>{throw new Error("should not update")}};await expect(createInventoryReservationService(repo).release("t","r1")).rejects.toThrow("Only RESERVED");});it("consumes reserved inventory",async()=>{let status="RESERVED";const repo={get:async()=>({id:"r1",tenantId:"t",orderId:"o",productId:"p",quantity:1,status:status as any}),reserve:async()=>({id:"r1",tenantId:"t",orderId:"o",productId:"p",quantity:1,status:"RESERVED" as const}),updateStatus:async(_t:string,_i:string,s:any)=>{status=s;return {id:"r1",tenantId:"t",orderId:"o",productId:"p",quantity:1,status:s}}};await createInventoryReservationService(repo).consume("t","r1");expect(status).toBe("CONSUMED");});});
