{
  "name": "@omnix/worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node --test"
  },
  "dependencies": {
    "@omnix/database": "workspace:*",
    "@omnix/queue": "workspace:*",
    "bullmq": "^5.58.5"
  }
}